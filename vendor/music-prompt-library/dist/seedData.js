import { join } from "node:path";
export const defaultProfilesPath = join(process.cwd(), "data", "music_profiles.json");
export const defaultAdaptersPath = join(process.cwd(), "data", "agent_adapters.json");
